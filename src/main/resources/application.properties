spring.application.name=demoSringbootST5ST6-2

# Database SQL Server (thay driver và thêm dialect)
spring.datasource.url=jdbc:sqlserver://localhost\\DUYSQL:1433;databaseName=WEBBT0602;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=40938813dD#
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver  # Thay từ MySQL sang SQL Server

# JPA/Hibernate (thêm dialect SQL Server, bỏ comment nếu cần)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect  # Thêm để tránh auto-detect fail

# Thymeleaf (từ PDF)
spring.thymeleaf.cache=false

# i18n (từ PDF)
spring.messages.basename=i18n/messages
spring.messages.encoding=UTF-8
spring.mvc.locale-resolver=fixed
spring.mvc.locale=vi_VN

# File Upload (từ PDF CRUD - đồng bộ với storage.location)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
storage.location=uploads  # Tạo thư mục uploads trong root project

server.port=9000